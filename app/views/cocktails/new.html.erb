<%= simple_form_for [cocktail] do |c| %>
  <%= c.label :name %>
  <%= c.text_field :name %>
  <%= c.label :photo %>
  <%= f.input :photo, as: :file %> 
  <%= c.submit %>
<% end %>

<%= link_to 'Back to list', root_path %>
